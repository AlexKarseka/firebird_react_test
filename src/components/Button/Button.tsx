import { type FC, type ReactElement } from "react";

import classes from "./Button.module.scss";

export const Button: FC = (): ReactElement => {
    return (
        <div className={classes.wrapper}>

        </div>
    );
};
